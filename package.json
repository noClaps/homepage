{
  "name": "zerolimits.dev",
  "scripts": {
    "build": "pnpx rolldown-vite build src/ --outDir ../dist --emptyOutDir && cp -r public/* dist/",
    "dev": "pnpx rolldown-vite src/"
  },
  "type": "module"
}
