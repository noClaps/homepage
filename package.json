{
  "name": "zerolimits.dev",
  "scripts": {
    "build": "deno run -A npm:vite build src/ --outDir ../dist --emptyOutDir && cp -r public/* dist/",
    "dev": "deno run -A npm:vite src/"
  },
  "type": "module"
}
