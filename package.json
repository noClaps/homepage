{
  "name": "zerolimits.dev",
  "scripts": {
    "build": "deno run -A npm:rolldown-vite build src/ --outDir ../dist --emptyOutDir && cp -r public/* dist/",
    "dev": "deno run -A npm:rolldown-vite src/"
  },
  "type": "module"
}
